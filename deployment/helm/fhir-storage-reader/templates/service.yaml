{{- if not .Values.istio.enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhir-storage-reader.fullversionname" . }}
  labels:
    {{- include "fhir-storage-reader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.http }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "fhir-storage-reader.selectorLabels" . | nindent 4 }}
{{- end }}